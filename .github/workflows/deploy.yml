name: Deploy Bass Impulse to production

on:
  push:
    tags:
      - 'v*'

jobs:
  deploy:
    uses: vanm30/mv-actions/.github/workflows/deploy.yml@main
    with:
      env-vars: |
        {
          "NEXT_PUBLIC_APP_TEST": "${{ secrets.NEXT_PUBLIC_APP_TEST }}"
        }
