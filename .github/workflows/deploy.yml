name: Deploy Bass Impulse to production

on:
  push:
    tags:
      - 'v*'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "NEXT_PUBLIC_APP_TEST is ${{ secrets.NEXT_PUBLIC_APP_TEST }}"
  deploy:
    uses: vanm30-portfolio/actions/.github/workflows/deploy.yml@debug-secrets
    with:
      config: '{"app": "website"}'
